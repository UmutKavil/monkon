<VirtualHost *:80>
    ServerName localhost
    ServerAlias localhost *.monkon.local
    DocumentRoot /var/www/html

    <Directory /var/www/html>
        Options Indexes FollowSymLinks MultiViews
        AllowOverride All
        Require all granted

        <IfModule mod_rewrite.c>
            RewriteEngine On
            RewriteBase /
            RewriteCond %{REQUEST_FILENAME} !-f
            RewriteCond %{REQUEST_FILENAME} !-d
            RewriteRule ^(.*)$ index.php [L]
        </IfModule>
    </Directory>

    <FilesMatch \.php$>
        SetHandler "proxy:unix:/var/run/php-fpm.sock|fcgi://localhost"
    </FilesMatch>

    # PhpMyAdmin Virtual Directory
    <Directory /var/www/html/phpmyadmin>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    # Apache Modules
    <IfModule mod_version.c>
        <IfVersion >= 2.4>
            SetEnvIf Request_URI "/${PHP_SCRIPT_NAME}" proxy-fcgi-pathinfo=undef
        </IfVersion>
    </IfModule>
</VirtualHost>

# SSL Configuration (optional, commented out by default)
# <VirtualHost *:443>
#     ServerName localhost
#     ServerAlias localhost *.monkon.local
#     DocumentRoot /var/www/html
#
#     SSLEngine on
#     SSLCertificateFile /etc/apache2/certs/localhost.crt
#     SSLCertificateKeyFile /etc/apache2/certs/localhost.key
#
#     <Directory /var/www/html>
#         Options Indexes FollowSymLinks
#         AllowOverride All
#         Require all granted
#     </Directory>
#
#     <FilesMatch \.php$>
#         SetHandler "proxy:unix:/var/run/php-fpm.sock|fcgi://localhost"
#     </FilesMatch>
# </VirtualHost>
